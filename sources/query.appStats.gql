query appStats ( $accountID:ID! $timeFrame:TimeFrame! $measure:[Measure] $dimension:[Dimension] $appStatsFilter:[AppStatsFilter!] $appStatsPostAggFilter:[AppStatsPostAggFilter!] $appStatsSort:[AppStatsSort!] $includeEmptyDimension:Boolean $limit:Int $from:Int) {
	appStats ( accountID:$accountID timeFrame:$timeFrame measures:$measure dimensions:$dimension filters:$appStatsFilter postAggFilters:$appStatsPostAggFilter sort:$appStatsSort includeEmptyDimension:$includeEmptyDimension ) {
		id
		from
		to
		total
		totals
		records ( limit:$limit  from:$from  )  {
			fieldsUnitTypes
			fieldsMap
			trends
			prevTimeFrame
			flatFields
		}
	}	
}