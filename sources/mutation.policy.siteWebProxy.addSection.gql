mutation policySiteWebProxyAddSection ($accountId: ID!, $siteWebProxyPolicyMutationInput: SiteWebProxyPolicyMutationInput, $policyAddSectionInput: PolicyAddSectionInput!) {
	policy(accountId: $accountId) {
		siteWebProxy(input: $siteWebProxyPolicyMutationInput) {
			addSection(input: $policyAddSectionInput) {
				section {
					audit {
						updatedTime
						updatedBy
					}
					metadata {
						lock {
							lockingAdminEmail
							lockingTime
							lockingRevisionId
						}
					}
					section {
						id
						name
					}
					properties
				}
				status
				errors {
					errorMessage
					errorCode
				}
				revision {
					id
					name
					description
					changes
					createdTime
					updatedTime
				}
			}
		}
	}
}
